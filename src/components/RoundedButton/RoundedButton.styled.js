import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  rounded: {
    height: 45,
    paddingLeft: 30,
    paddingRight: 30,
    borderRadius: 30,
  },
}));
